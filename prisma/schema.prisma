
datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Blog {
  id      Int    @id @default(autoincrement())
  title   String
  summary String
  content String
  slug    String @unique
}